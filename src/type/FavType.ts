export interface FavShops{
    favId: string;
    userId: string;
    shopId: string;
}

export interface FavSearch{
    favId: string;
    userId: string;
    keywordId: string;
}
export interface FavCate{
    favId: string;
    userId: string;
    cateId: string;
}